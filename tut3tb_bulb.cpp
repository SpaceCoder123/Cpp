#include<iostream>
using namespace std;
int main(){
    char ch;

    cout<<"Enter keys, x to stop "<<endl;
    while (ch != 'x'){
        cin>>ch;
        cout<<": "<<ch<<endl;

    }
    
}

// this programt eaches you the use of double qoutes and single qoutes, if double qoutes are used then char name[number] should be defined and if the string has been input in single qoutes then only char name can be defined
// " " == > char name[number]
// ' ' == > char name

// this program ends as soon as the user gives an input of "x"; 
